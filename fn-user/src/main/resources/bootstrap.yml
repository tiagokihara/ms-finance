spring:
  application:
    name: fn-user
  cloud:
    config:
      uri: http://fn-config-server:8888
  profiles:
    active: prod
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
